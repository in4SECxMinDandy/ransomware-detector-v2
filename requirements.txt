# Ransomware Detector v2.4 — Runtime Dependencies
# Install: pip install -r requirements.txt
#
# Development extras (pytest, ruff, pip-audit, ...) live in requirements-dev.txt.
# This file is intentionally trimmed so production images never pull dev tools.

# ─── Core ML / Data ────────────────────────────────────────────────────────────
scikit-learn>=1.3.0       # RandomForest, CalibratedClassifierCV, metrics
numpy>=1.24.0            # Feature extraction, entropy computation
pandas>=2.0.0             # Data analysis, CSV export
scipy>=1.11.0             # Scientific computing

# ─── Malware Detection ────────────────────────────────────────────────────────
psutil>=5.9.0             # Process monitoring, IO counters, network
# yara-python>=4.3.0      # Optional — native YARA engine (much faster)
                        # pip install yara-python  (requires YARA dev headers)

# ─── Filesystem Monitoring ────────────────────────────────────────────────────
watchdog>=3.0.0           # Real-time filesystem watcher (RealTimeMonitor)

# ─── Office Document Analysis ──────────────────────────────────────────────────
oletools>=0.60            # VBA macro extraction (olevba, mraptor, rtfobj)
PyMuPDF>=1.23             # PDF analysis (OpenAction, JavaScript, /AA, /Launch)
python-docx>=1.1           # DOCX analysis
openpyxl>=3.1              # XLSX/XLSM analysis
python-pptx>=0.6           # PPTX analysis
pdfminer.six>=20231228     # PDF text extraction (fallback)

# ─── GUI (CustomTkinter — lightweight, modern dark theme) ────────────────────
customtkinter>=5.2.0         # Modern dark-themed Tkinter wrapper
Pillow>=10.0.0               # Image handling (for GUI icons)

# ─── Reporting / Export ───────────────────────────────────────────────────────
matplotlib>=3.7.0         # Charts, PNG export, PR curves
reportlab>=4.0.0          # PDF report generation
joblib>=1.3.0              # Model serialization

# ─── Notifications ────────────────────────────────────────────────────────────
win10toast>=0.9            # Windows 10/11 toast notifications
plyer>=2.1.0              # Cross-platform notifications (fallback)

# ─── System Tray ──────────────────────────────────────────────────────────────
pystray>=0.19.5            # System tray integration

# ─── Threat Intelligence (optional) ────────────────────────────────────────────
stix2>=3.0.0               # STIX/TAXII threat intel format
scapy>=2.5.0              # Network packet analysis (advanced C2 detection)

# ─── Imbalanced Learning ──────────────────────────────────────────────────────
imbalanced-learn>=0.11.0   # SMOTE oversampling for class imbalance

# ─── FastAPI & API Server ─────────────────────────────────────────────────────
fastapi>=0.109             # REST API framework
uvicorn[standard]>=0.27   # ASGI server
python-multipart>=0.0.9   # File upload support
python-jose[cryptography]>=3.3  # JWT token handling
passlib[bcrypt]>=1.7      # Password hashing
pydantic>=2.5             # Data validation
pydantic-settings>=2.0     # Settings management

# ─── HTTP Clients ────────────────────────────────────────────────────────────
httpx>=0.27.0              # HTTP client for threat intelligence integrations
requests>=2.31.0           # HTTP client (VirusTotal and related APIs)

# Testing dependencies have been moved to requirements-dev.txt.
# Production deployments must NOT install pytest / coverage tooling.
