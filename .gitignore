__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
ENV/
env/
.env
.pytest_cache/
.coverage
htmlcov/
dist/
build/
*.egg-info/
.vscode/
.idea/
*.swp
*.bak
~$*
.DS_Store

logs/
debug_err.txt
debug_out.txt
*.log

# Data and Models
data/*.csv
data/*.jsonl
data/config.json
data/honeypot_registry.json
data/ti_cache.json
data/vt_cache.json
data/_prep_out/
data/_prep_src/
data/_tmp_encrypted/
data/_tmp_safe/
models/*.joblib
models/model_metadata*.json
!models/rf_ransomware_detector.joblib

# Malware/Decoy files (untracked — kept local only)
Malware/
ransomeware/
*.elf
*.quarantined

# Quarantine artefacts (live attack data — never commit)
quarantine/
!quarantine/.gitkeep

# Local agent / debug traces
debug-*.log
debug_err.txt
debug_out.txt

# Tooling
.windsurf/
datasets/
external_pe/

# Local report exports
reports/*.csv
reports/*.png
reports/*.pdf
reports/*.json
!reports/.gitkeep

# Root-level data files that have been moved to data/
# (keep rules here so old paths are still ignored if they reappear)
malware_analysis.json
pkg_list.txt
full.csv
ransomware_files.txt

# scripts/cmd — batch scripts (tracked but not needed in CI artefacts)
# scripts/dev/manual_tests — manual smoke tests, not part of pytest suite

